import React from 'react';
import PageTemplate from '@/components/PageTemplate';

const NumberSeries = () => (
  <PageTemplate title="Number Series" description="Configure document numbering schemes" testId="number-series-page" />
);

export default NumberSeries;
